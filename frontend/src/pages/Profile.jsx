import React from 'react';

const Profile = () => (
  <div>
    <h1>User Profile & Orders</h1>
    <p>Track orders, manage subscriptions, and view loyalty points.</p>
  </div>
);

export default Profile;
